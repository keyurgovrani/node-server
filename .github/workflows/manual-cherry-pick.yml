name: Manual Cherry-pick

run-name: Cherry-pick ${{ github.event.inputs.commit-hash }} from ${{ github.ref_name }} to ${{ github.event.inputs.dest-branch-choice == 'custom' && github.event.inputs.custom-dest-branch || github.event.inputs.dest-branch-choice }}

on:
  workflow_dispatch:
    inputs:
      dest-branch-choice:
        description: "Choose destination branch or select 'custom'"
        required: true
        type: choice
        default: "uat"
        options:
          - uat
          - live
          - custom

      custom-dest-branch:
        description: "Enter custom destination branch name (if 'custom' selected)"
        required: false
        type: string

      commit-hash:
        description: "Commit hash to cherry-pick"
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  manual-cherry-pick:
    runs-on: ubuntu-latest

    steps:

      - name: Set workflow variables
        id: vars
        run: |
          COMMIT_HASH=$(echo "${{ github.event.inputs.commit-hash }}" | xargs)
          DEST_BRANCH_CHOICE=$(echo "${{ github.event.inputs.dest-branch-choice }}" | xargs)
          CUSTOM_DEST_BRANCH=$(echo "${{ github.event.inputs.custom-dest-branch }}" | xargs)
          REF_NAME=$(echo "${{ github.ref_name }}" | xargs)
          
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_OUTPUT
          echo "DEST_BRANCH_CHOICE=$DEST_BRANCH_CHOICE" >> $GITHUB_OUTPUT
          echo "CUSTOM_DEST_BRANCH=$CUSTOM_DEST_BRANCH" >> $GITHUB_OUTPUT
          echo "REF_NAME=$REF_NAME" >> $GITHUB_OUTPUT

      - name: Determine destination branch
        id: branch
        run: |
          if [[ "${{ steps.vars.outputs.DEST_BRANCH_CHOICE }}" == "custom" ]]; then
            if [[ -z "${{ steps.vars.outputs.CUSTOM_DEST_BRANCH }}" ]]; then
              echo "Custom branch option selected but no custom branch provided."
              exit 1
            fi
            echo "DEST=${{ steps.vars.outputs.CUSTOM_DEST_BRANCH }}" >> $GITHUB_OUTPUT
          else
            echo "DEST=${{ steps.vars.outputs.DEST_BRANCH_CHOICE }}" >> $GITHUB_OUTPUT
          fi

          echo "Base branch (workflow run branch): ${{ steps.vars.outputs.REF_NAME }}"
          echo "Destination branch: $(echo $DEST)"

      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"

      - name: Validate destination branch exists
        run: |
          if git ls-remote --heads origin ${{ steps.branch.outputs.DEST }} \
            | grep -q "${{ steps.branch.outputs.DEST }}"; then
            echo "Destination branch exists."
          else
            echo "❌ Branch '${{ steps.branch.outputs.DEST }}' does not exist!"
            exit 1
          fi

      - name: Fetch all branches
        run: git fetch --all

      - name: Checkout destination branch
        run: |
          git checkout ${{ steps.branch.outputs.DEST }}
          git pull origin ${{ steps.branch.outputs.DEST }}

      - name: Cherry-pick commit
        id: cherry-pick
        run: |
          COMMIT="${{ steps.vars.outputs.COMMIT_HASH }}"

          # Count parents — merge commits have 2 parents
          PARENT_COUNT=$(git show --pretty=%P -s $COMMIT | wc -w)

          if [ "$PARENT_COUNT" -gt 1 ]; then
            echo "Merge commit detected → using: git cherry-pick -m 1 $COMMIT"
            PICK_CMD="git cherry-pick -m 1 $COMMIT"
          else
            echo "Normal commit → using: git cherry-pick $COMMIT"
            PICK_CMD="git cherry-pick $COMMIT"
          fi

          if $PICK_CMD; then
            echo "status=success" >> $GITHUB_OUTPUT
            echo "Cherry-pick successful"
          else
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "Cherry-pick failed"

            if git status --porcelain | grep -q "^UU"; then
              echo "Conflicts detected → aborting"
              git cherry-pick --abort
            fi

            exit 1
          fi

      - name: Push commit
        if: steps.cherry-pick.outputs.status == 'success'
        run: git push origin ${{ steps.branch.outputs.DEST }}

      - name: Find PR associated with commit
        id: pr-lookup
        uses: actions/github-script@v7
        with:
          script: |
            const commit = "${{ steps.vars.outputs.COMMIT_HASH }}";
            const { owner, repo } = context.repo;

            const prs = await github.rest.repos.listPullRequestsAssociatedWithCommit({
              owner,
              repo,
              commit_sha: commit
            });

            core.setOutput("pr", prs.data[0]?.number ?? "");

      - name: Comment on PR (if found)
        if: steps.pr-lookup.outputs.pr != ''
        uses: actions/github-script@v7
        with:
          script: |
            const pr = ${{ steps.pr-lookup.outputs.pr }};
            const { owner, repo } = context.repo;
            const dest = "${{ steps.branch.outputs.DEST }}";
            const commitHash = "${{ steps.vars.outputs.COMMIT_HASH }}";
            const status = "${{ steps.cherry-pick.outputs.status }}";

            let comment = '';

            if (status === "success") {
                comment = `✅ **Cherry-pick Successful**\n\nCommit \`${commitHash}\` has been cherry-picked into the \`${dest}\` branch.`;
            } else {
                comment = `❌ **Cherry-pick Failed**\n\nFailed to cherry-pick commit \`${commitHash}\` into the \`${dest}\` branch due to conflicts. Manual intervention may be required.`;
            }

            await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: pr,
                body: comment
            });

      - name: No PR found
        if: steps.pr-lookup.outputs.pr == ''
        run: echo "ℹ️ No PR found for commit."

      - name: Fail workflow if cherry-pick failed
        if: steps.cherry-pick.outputs.status == 'failed'
        run: exit 1
